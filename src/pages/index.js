import React from 'react';

import { Layout, Hero, HeroIndex } from '../components';

const Home = () => {
  return (
    <Layout>
      <Hero>
        <HeroIndex />
      </Hero>
    </Layout>
  );
};

export default Home;
